C4Container
    title Level 2: Container Diagram

    Person(user, "Developer / DevOps", "System user")
    
    System_Boundary(platform, "Centralized Log Aggregation Platform") {
        Container(api, "API Collector", "Python (FastAPI)", "Accepts JSON, validates, writes to DB")
        Container(db, "Database", "MariaDB", "Stores logs and metadata")
        Container(worker, "Alerting Worker", "Python (Threading)", "Analyzes logs, sends SMTP alerts")
    }
    
    System_Ext(smtp, "SMTP Server", "External mail service")

    Rel(user, api, "HTTP API")
    Rel(api, db, "Write Log")
    Rel(db, worker, "Read for check")
    Rel(worker, smtp, "Send Email")