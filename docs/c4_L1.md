C4Context
    title Level 1: System Context - Centralized Log Aggregation Platform

    Person(devops, "DevOps Engineer", "Monitors infrastructure status, analyzes incidents")
    Person(dev, "Developer", "Uses logs for application debugging")
    
    System(platform, "Centralized Log Aggregation Platform", "Centralized collection, storage, classification, and alerting")
    
    System_Ext(microservices, "Microservices / Applications", "Data sources")
    System_Ext(smtp, "SMTP Service", "External mail provider")

    Rel(microservices, platform, "Send logs (HTTP POST)")
    Rel(platform, smtp, "Sends notifications (SMTP)")
    Rel(devops, platform, "Analyzes logs, configures alerts")
    Rel(dev, platform, "Requests logs for debugging")